---
local_user: mike
local_user_home: /home/{{ local_user }}
# git config
git_email: "mmamaenko@yahoo.com"
git_user: "Mike Mamaenko"

# projects
project_base: "{{ local_user_home }}/projects"
python_venv: "{{ project_base }}/python/.venv"
python_venv_prj: "{{ project_base }}/python/prj"
project_tree:
    - python
    - python/.venv
    - python/prj
    - chef
    - chef/repo
    - ansible
    - ansible/roles
    - ansible/vars
    - ansible/group_vars
    - ansible/host_vars
    - ansible/library
    - ansible/logs

# Python based version
python_version: "2.7.5"

